# Fine-tuned model checkpoints — local-only.
# DistilBERT checkpoints are ~265 MB, exceeding GitHub's 100 MB hard limit.
# The model lives on Hugging Face Hub; do not commit it to the repo.
model/
fixed_model/
checkpoint-*/
# Python
__pycache__/
*.pyc
.pytest_cache/

# Local environment
.env
.venv/
venv/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/
